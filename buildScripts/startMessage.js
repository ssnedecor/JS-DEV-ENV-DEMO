//var chalk = require('chalk');  //ES5

import chalk from 'chalk'; //ES6 - node doesn't understand. Reason for babel.


console.log(chalk.green('Starting app in dev mode...')); //eslint-disable-line no-console
